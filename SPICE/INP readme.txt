These functions encapsualte many of the common operations the input package needs to perform
frequently which are conceptually simply, but require a moderate amount of code which would 
otherwise be needlessly replicated.